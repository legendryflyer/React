let firstname = "tavish"
let lastname = "anade"


console.log(`My fullname is ${firstname} ${lastname}.`); // My full name is tavish anade.

// You can also use template literals to include variables inside a string by using the backtick (`) instead of single or double quotes.
// You can also use template literals to include variables inside a string by using the backtick (`) instead of quotes (''). 


// template litrals also works with expressions

console.log(`1+4=${1+4}`);  //  5=5

